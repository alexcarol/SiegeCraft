{% extends '::base.html.twig' %}

{% block body %}
<h1>ConfigTechnology creation</h1>

<form action="{{ path('configtechnology_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('configtechnology') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}