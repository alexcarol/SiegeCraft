<h1>ConfigTechnology</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ entity.name }}</td>
        </tr>
        <tr>
            <th>Description</th>
            <td>{{ entity.description }}</td>
        </tr>
        <tr>
            <th>Multiplier</th>
            <td>{{ entity.multiplier }}</td>
        </tr>
        <tr>
            <th>Costs</th>
            {% for type, cost in entity.costs %}
                <td>{{ type }} : {{ cost }}</td>
            {% endfor %}
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('configtechnology') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('configtechnology_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('configtechnology_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
